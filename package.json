{
  "name": "the-thing",
  "private": true,
  "scripts": {
    "dev": "concurrently \"lerna run dev --scope=api\"  \"lerna run dev --scope=client\"",
    "api": "lerna run dev --scope=api"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "devDependencies": {
    "lerna": "^8.2.2"
  }
}
